{
  "version": "1.0.0",
  "configurations": [
    {
      "name": "Launch: Catalog Service",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/cmd/main.go",
      "envFile": "${workspaceFolder}/.env",
    }
  ]
}
